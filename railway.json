{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": ["sh -c \"npx prisma migrate resolve --rolled-back 20260606193000_clerk_app_users 2>/dev/null || true && npx prisma migrate deploy\""],
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

